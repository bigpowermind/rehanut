.CommentBox
  .CommentInfo
    .CommentInfo__userName
      = comment.user.name
    .CommentInfo__date
      = comment.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Comment
    - if comment.content.present?
      %p.Comment__content
        = comment.content
    = image_tag content.image.url, class: 'Comment__image' if comment.image.present?