.MessageBox
  .MessageInfo
    .MessageInfo__userName
      = comment.user.name
    .MessageInfo__date
      = comment.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Message
    - if comment.comment.present?
      %p.Message__text
        = comment.comment
    = image_tag comment.image.url, class: 'Message__image' if comment.image.present?